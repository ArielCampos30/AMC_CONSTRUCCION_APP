import {createFloatingChat} from './floating-chat.js';
import {chatViewport,previewChatPhotos} from './chat-view.js';
import {buildClientContacts,contactForRequest,messagesForContact} from './chat-client-thread.js';

export function createChatFeatures({getState,isAdmin,esc,heading,empty,thumb,api,upload,navigate,toast,onNoticesRead=()=>{}}){
 let threadId='',activeClientContact=null,preferredRequestId='';
 const uploadedPhotos=new WeakMap(),limits=new Map(),pending=new Map();
 const s=()=>getState(),requests=()=>s().chatRequests||s().requests||[],key=()=>crypto.randomUUID();
 const paintPending=x=>{const log=document.querySelector('.message-log'),follow=log?log.scrollHeight-log.clientHeight-log.scrollTop<90:false;let el=document.querySelector(`[data-pending-id="${x.id}"]`);if(!el){el=document.createElement('article');el.className='message mine optimistic';el.dataset.pendingId=x.id;log?.append(el);}el.classList.toggle('failed',x.state==='Error');const status=x.state==='Error'?`<small class="message-meta">Error</small><button type="button" class="retry-message" data-pending-id="${x.id}">Reintentar</button>`:`<small class="message-meta upload-state" aria-label="Enviando"><span class="message-upload-spinner" aria-hidden="true"></span>${x.progress>0?'<span>'+x.progress+'%</span>':''}</small>`;el.innerHTML=`<p>${esc(x.text).replace(/\n/g,'<br>')}</p><div class="mini-photos">${x.urls.map(url=>`<img src="${url}" alt="Foto pendiente">`).join('')}</div>${status}`;if(follow)log.scrollTop=log.scrollHeight;};
 const releasePending=x=>{x.urls.forEach(URL.revokeObjectURL);pending.delete(x.id);document.querySelector(`[data-pending-id="${x.id}"]`)?.remove();};
 const floatingClientContacts=()=>buildClientContacts({requests:requests(),messages:s().messages||[],chatUnread:s().chatUnread||{},agendaClients:s().agendaClients||[],admin:isAdmin()});
 const clientContactForRequest=requestId=>contactForRequest(floatingClientContacts(),requestId);
 const selectClientContact=(contact,requestId='')=>{if(!contact)return false;activeClientContact=contact;threadId=contact.requestIds?.includes(requestId)?requestId:(contact.replyRequestId||contact.requestIds?.[0]||threadId);chatViewport.open();return true;};
 const ensureClientContact=()=>{if(activeClientContact&&activeClientContact.requestIds?.some(id=>requests().some(r=>r.id===id)))return activeClientContact;const contacts=floatingClientContacts(),found=threadId?contactForRequest(contacts,threadId):null;activeClientContact=found||contacts[0]||null;if(activeClientContact&&!activeClientContact.requestIds?.includes(threadId))threadId=activeClientContact.replyRequestId||activeClientContact.requestIds?.[0]||'';return activeClientContact;};
 const currentLimitKey=()=>activeClientContact?'client:'+activeClientContact.id:'request:'+threadId;

 function chat(contact){
  if(contact?.requestIds?.length)selectClientContact(contact,preferredRequestId||threadId);const selected=ensureClientContact();chatViewport.capture(threadId);if(!selected)return heading('CONVERSACIONES','Mensajes')+empty('Todavía no tenés conversaciones','Cuando envíes una solicitud podrás hablar directamente con AMC Construcciones.');
  const requestIds=selected.requestIds||[],requestMap=new Map(requests().filter(r=>requestIds.includes(r.id)).map(r=>[r.id,r])),allMessages=messagesForContact(selected,s().messages||[]),limitKey=currentLimitKey(),limit=limits.get(limitKey)||60,messages=allMessages.slice(-limit),older=allMessages.length-messages.length;
  const byRequest=new Map(requestIds.map(id=>[id,(s().messages||[]).filter(m=>m.requestId===id).sort((a,b)=>a.date.localeCompare(b.date)||a.id.localeCompare(b.id))]));
  const readThrough=new Map(requestIds.map(id=>{const rows=byRequest.get(id)||[],marker=s().chatReadByOther?.[id];return [id,marker?rows.findIndex(m=>m.id===marker):-1];}));
  const rows=messages.map(m=>{const request=requestMap.get(m.requestId),requestMessages=byRequest.get(m.requestId)||[],mine=m.senderId===s().user.id,index=requestMessages.findIndex(x=>x.id===m.id),read=mine&&(readThrough.get(m.requestId)??-1)>=0&&index>=0&&index<=(readThrough.get(m.requestId)??-1),time=new Date(m.date).toLocaleTimeString('es-AR',{hour:'2-digit',minute:'2-digit'}),check=mine?`<span class="message-check ${read?'read':''}" title="${read?'Leído':'Enviado'}" aria-label="${read?'Leído':'Enviado'}">✓</span>`:'',context=requestIds.length>1?`<small class="message-project-context">${esc(request?.service||'Proyecto')}</small>`:'';return `<article class="message ${mine?'mine':''}" data-message-id="${m.id}" data-request-id="${m.requestId}">${context}<strong>${esc(m.senderName)}${m.senderRole==='admin'?' · AMC':''}</strong><p>${esc(m.text).replace(/\n/g,'<br>')}</p>${thumb(m.photos,{sender:m.senderName,date:m.date})}<small class="message-meta" title="${esc(new Date(m.date).toLocaleString('es-AR'))}"><span>${time}</span>${check}</small></article>`}).join('');
  const optimistic=[...pending.values()].filter(x=>requestIds.includes(x.requestId)).map(x=>{const request=requestMap.get(x.requestId),context=requestIds.length>1?`<small class="message-project-context">${esc(request?.service||'Proyecto')}</small>`:'',status=x.state==='Error'?`<small class="message-meta">Error</small><button type="button" class="retry-message" data-pending-id="${x.id}">Reintentar</button>`:`<small class="message-meta upload-state" aria-label="Enviando"><span class="message-upload-spinner" aria-hidden="true"></span>${x.progress>0?'<span>'+x.progress+'%</span>':''}</small>`;return `<article class="message mine optimistic ${x.state==='Error'?'failed':''}" data-pending-id="${x.id}">${context}<p>${esc(x.text).replace(/\n/g,'<br>')}</p><div class="mini-photos">${x.urls.map(url=>`<img src="${url}" alt="Foto pendiente">`).join('')}</div>${status}</article>`}).join('');
  const replyRequest=requestMap.get(threadId)||requestMap.get(selected.replyRequestId)||requestMap.values().next().value,replyRequestId=replyRequest?.id||selected.requestIds?.[0]||'';threadId=replyRequestId;
  const title=isAdmin()?selected.name:'AMC Construcciones',subtitle=isAdmin()?(requestIds.length>1?`${requestIds.length} proyectos · respondiendo sobre ${replyRequest?.service||'Proyecto'}`:(replyRequest?.service||'Conversación con el cliente')):(requestIds.length>1?'Historial completo de tus proyectos':'Conversación directa con Administración');
  return heading('TODO EN UN SOLO LUGAR',isAdmin()?'Chat del cliente':'AMC Construcciones')+`<section class="conversation panel"><div class="conversation-title"><h2>${esc(title)}</h2><span>${esc(subtitle)}</span></div><div class="message-log" role="log" aria-label="Historial de mensajes del cliente">${older?`<button type="button" class="load-older">Cargar ${Math.min(60,older)} mensajes anteriores</button>`:''}${rows}${optimistic||(!messages.length?'<div class="empty-conversation">Este es el espacio para mantener el historial de esta conversación.</div>':'')}</div><form class="message-form" data-request="${replyRequestId}" data-key="${key()}"><label>Tu mensaje<textarea name="text" rows="3" maxlength="4000" placeholder="Escribí un mensaje…"></textarea></label><label>Agregar fotos (hasta 4)<input name="photos" type="file" multiple accept="image/jpeg,image/png,image/webp"></label><button type="submit" class="primary">Enviar mensaje</button></form></section>`;
 }

 async function submit(form,data){
  if(!form.classList.contains('message-form'))return false;
  if(form.dataset.sending==='1')return 'message-sent';
  const senderId=s().user?.id,sendButton=form.querySelector('button[type=submit]'),files=[...form.elements.photos.files];
  if(files.length>4)throw Error('Elegí hasta cuatro fotos.');
  if(!String(data.text).trim()&&!files.length)throw Error('Escribí un mensaje o adjuntá una foto.');
  form.dataset.sending='1';if(sendButton)sendButton.disabled=true;
  let item=[...pending.values()].find(x=>x.form===form);if(!item){item={id:key(),requestId:form.dataset.request,form,text:String(data.text||''),files,urls:files.map(URL.createObjectURL),state:'Enviando',progress:0};pending.set(item.id,item);}else{item.state='Enviando';item.progress=0;}
  const preview=form.querySelector('.chat-photo-preview');if(preview)preview.hidden=true;paintPending(item);
  let status=form.querySelector('.chat-send-status');if(!status){status=document.createElement('p');status.className='chat-send-status';status.setAttribute('role','status');status.hidden=true;form.append(status);}
  try{
   const photos=[];for(const file of item.files){let saved=uploadedPhotos.get(file);if(!saved||saved.userId!==senderId){const result=await upload(file,percent=>{item.progress=percent;paintPending(item);},false);saved={id:result.id,userId:senderId};uploadedPhotos.set(file,saved);}photos.push(saved.id);}
   if(s().user?.id!==senderId)throw Error('La sesión cambió. Volvé a ingresar antes de enviar.');
   item.state='Enviando';item.progress=100;paintPending(item);
   const message=await api('/api/requests/'+form.dataset.request+'/messages',{text:item.text,photos,idempotencyKey:form.dataset.key});
   if(s().user?.id!==senderId)return 'message-sent';
   if(!s().messages.some(m=>m.id===message.id))s().messages.push(message);
   item.state='Enviado';paintPending(item);form.reset();previewChatPhotos(form.elements.photos);if(form.classList.contains('compact-composer'))form.elements.text.style.height='40px';form.querySelector('.chat-photo-preview')?.remove();form.dataset.key=key();form.dataset.sending='0';if(sendButton)sendButton.disabled=false;status.textContent='';status.hidden=true;setTimeout(()=>releasePending(item),180);chatViewport.open();setTimeout(updateChat,360);return 'message-sent';
  }catch(error){form.dataset.sending='0';if(sendButton)sendButton.disabled=false;releasePending(item);if(preview)preview.hidden=false;const raw=String(error?.message||''),friendly=/source image could not be decoded|decode|encodingerror|invalidstateerror/i.test(raw)?'No pudimos preparar esta foto. Probá con otra imagen o sacala nuevamente.':(raw||'No se pudo enviar. La foto sigue adjunta para volver a intentar.');status.hidden=false;status.textContent=friendly;throw Error(friendly);}
 }

 function change(){return false;}
 function selectChat(requestId){const contact=clientContactForRequest(requestId);if(!contact)return false;preferredRequestId=requestId;return selectClientContact(contact,requestId);}
 function openChat(requestId){if(['admin','client'].includes(s().user?.role)){if(!selectChat(requestId))return toast('El chat está disponible para tus clientes y proyectos.');floating.open(true);return true;}threadId=requestId;chatViewport.open();navigate('mensajes');return true;}
 function afterRender(page){const fullPageChat=(s().user?.role==='client'&&(page==='chat-cliente'||page.startsWith('chat/')))||(s().user?.role==='admin'&&(page==='chat-admin'||page.startsWith('chat-admin/'))),quotePage=page==='cotizador';document.body.classList.toggle('full-chat-page',fullPageChat);document.body.classList.toggle('client-chat-page',s().user?.role==='client'&&fullPageChat);document.body.classList.toggle('quote-wizard-route',quotePage);if(fullPageChat||quotePage)floating.close?.();const chatPage=['chat-cliente','chat-admin'].includes(page)||page.startsWith('chat/')||page.startsWith('chat-admin/')?'mensajes':page;syncChatAccess();chatViewport.mount(chatPage,threadId);}
 function updateChat(){if(!document.querySelector('.message-log'))return;const box=document.createElement('div');box.innerHTML=chat();const next=box.querySelector('.message-log'),current=document.querySelector('.message-log');if(next&&current.innerHTML!==next.innerHTML){current.replaceWith(next);chatViewport.mount('mensajes',threadId);}markRead();}

 document.addEventListener('click',e=>{const older=e.target.closest('.load-older');if(older){const log=older.closest('.message-log'),height=log.scrollHeight,key=currentLimitKey();limits.set(key,(limits.get(key)||60)+60);updateChat();const next=document.querySelector('.message-log');if(next)next.scrollTop=next.scrollHeight-height;return;}const retry=e.target.closest('.retry-message');if(retry){const item=pending.get(retry.dataset.pendingId),form=item?.form?.isConnected?item.form:document.querySelector(`.message-form[data-request="${item?.requestId||''}"]`);if(form){item.form=form;form.requestSubmit();}}});
 document.addEventListener('change',e=>{if(e.target.matches('.message-form input[type=file]'))previewChatPhotos(e.target);});

 function floatingEmployeeContacts(){return (s().employees||[]).filter(e=>e.role==='employee'&&e.active!==false).map(employee=>{const messages=(s().staffMessages||[]).filter(m=>m.employeeId===employee.id).sort((a,b)=>b.date.localeCompare(a.date)),last=messages[0];return {id:employee.id,kind:'employee',name:employee.name,service:employee.specialty||'Equipo AMC',unread:0,last:last?.text||(last?.photos?.length?'Foto':'Sin mensajes'),date:last?.date||''};});}
 function floatingStaffThread(contact){const messages=(s().staffMessages||[]).filter(m=>m.employeeId===contact.id).sort((a,b)=>a.date.localeCompare(b.date)),readAt=s().staffReadByEmployee?.[contact.id]||'';return '<section class="conversation"><div class="message-log" role="log" aria-label="Mensajes con '+esc(contact.name)+'">'+(messages.map(m=>{const mine=m.senderRole==='admin',read=mine&&readAt&&m.date<=readAt;return '<article class="message '+(mine?'mine':'')+'" data-message-id="'+esc(m.id)+'"><strong>'+esc(m.senderName||contact.name)+'</strong><p>'+esc(m.text||'').replace(/\n/g,'<br>')+'</p><small class="message-meta"><span>'+new Date(m.date).toLocaleTimeString('es-AR',{hour:'2-digit',minute:'2-digit'})+'</span>'+(mine?'<span class="message-check '+(read?'read':'')+'" title="'+(read?'Leído':'Enviado')+'">✓</span>':'')+'</small></article>'}).join('')||'<div class="empty-conversation">Sin mensajes todavía.</div>')+'</div><form class="message-form floating-staff-message"><textarea name="text" rows="1" maxlength="4000" placeholder="Mensaje…"></textarea><button type="submit" class="primary">Enviar</button></form></section>';}

 const floating=createFloatingChat({
  allowed:()=>isAdmin()?((requests().length>0)||floatingEmployeeContacts().length>0):requests().length>0,
  label:()=>{const n=Object.values(s().chatUnread||{}).reduce((a,b)=>a+b,0);return (n?'🟢 '+n+' sin leer · ':'◌ ')+(isAdmin()?'Chats':'Conversar con AMC');},
  groups:()=>isAdmin()?[{id:'clients',label:'Clientes',subtitle:'Historial completo por cliente',searchPlaceholder:'Buscar cliente'},{id:'employees',label:'Empleados',subtitle:'Equipo AMC',searchPlaceholder:'Buscar empleado'}]:[],
  contacts:group=>isAdmin()&&group==='employees'?floatingEmployeeContacts():floatingClientContacts(),
  current:()=>{const contact=clientContactForRequest(threadId)||ensureClientContact();return contact?.id||'';},
  select:(id,contact)=>{if(contact?.kind!=='employee'){const requested=preferredRequestId&&contact.requestIds?.includes(preferredRequestId)?preferredRequestId:'';selectClientContact(contact,requested);preferredRequestId='';}},
  renderChat:contact=>chat(contact),
  customThread:contact=>contact?.kind==='employee'?floatingStaffThread(contact):'',
  submitCustom:async(contact,payload)=>{const message=await api('/api/staff-chat/messages',{employeeId:contact.id,text:payload.text,idempotencyKey:payload.idempotencyKey});if(!(s().staffMessages||[]).some(x=>x.id===message.id))s().staffMessages.push(message);return message;},
  mounted:contact=>{if(contact?.kind==='employee'){if(isAdmin())api('/api/staff-chat/read',{employeeId:contact.id}).then(result=>onNoticesRead(result.noticeIds||[])).catch(()=>{});return;}if(contact?.requestIds?.length)activeClientContact=contact;updateChat();chatViewport.mount('mensajes',threadId);markRead();}
 });
 function syncChatAccess(){floating.sync(s().user?.id);if((floating.isOpen()&&floating.kind()!=='employee')||document.body.classList.contains('full-chat-page'))updateChat();}
 const reading=new Set();function markRead(){if(floating.kind()==='employee')return;const log=document.querySelector('.message-log'),uid=s().user?.id,contact=ensureClientContact();if(document.hidden||!uid||!log?.getClientRects().length||!contact||log.scrollHeight-log.clientHeight-log.scrollTop>90)return;for(const id of contact.requestIds||[]){const last=s().chatLatest?.[id];if(!last||!s().chatUnread?.[id]||reading.has(last)||!s().messages.some(m=>m.id===last&&m.requestId===id))continue;reading.add(last);api('/api/requests/'+id+'/messages/read',{lastMessageId:last}).then(result=>{onNoticesRead(result.noticeIds||[]);if(s().user?.id===uid&&s().chatLatest?.[id]===last){s().chatUnread[id]=0;floating.sync(uid);}}).catch(()=>{}).finally(()=>reading.delete(last));}}
 document.addEventListener('scroll',e=>{if(e.target.matches?.('.message-log'))markRead();},{capture:true,passive:true});
 window.AMCOpenChatRequest=requestId=>openChat(String(requestId||''));

 return {selectChat,syncChatAccess,updateChat,render:name=>name==='mensajes'?chat():undefined,submit,change,openChat,afterRender};
}

export function clientRows(state){
 const quotesByRequest=new Map((state.quotes||[]).map(q=>[q.requestId,q])),requests=new Map(),works=new Map(),archives=new Set(state.archivedClients||[]);
 for(const r of state.requests||[]){const key=r.leadId||r.userId;if(!key)continue;if(!requests.has(key))requests.set(key,[]);requests.get(key).push(r);}
 for(const w of state.works||[]){const key=w.userId;if(!key)continue;if(!works.has(key))works.set(key,[]);works.get(key).push(w);}
 return (state.agendaClients||state.clients||[]).map(c=>{const rs=requests.get(c.id)||[],ws=works.get(c.id)||[],qs=rs.map(r=>quotesByRequest.get(r.id)).filter(Boolean),active=rs.some(r=>!['Cerrada','No tomada'].includes(r.status))||ws.some(w=>w.status!=='Finalizado'||Number(w.budget||0)-(w.payments||[]).reduce((n,p)=>n+Number(p.amount||0),0)>0);return {...c,town:c.town||rs[0]?.town||'',phone:c.phone||rs[0]?.phone||'',requests:rs,works:ws,quotes:qs,group:active?'En proceso':archives.has(c.id)?'Archivados':'Historial'};}).sort((a,b)=>a.name.localeCompare(b.name,'es'));
}
export function createClientDirectory({getState,esc}){let filter='En proceso',query='',page=0;function rows(){return clientRows(getState()).filter(c=>c.group===filter&&[c.name,c.email,c.phone,c.town].join(' ').toLocaleLowerCase().includes(query.toLocaleLowerCase()));}
 return {rows,filter:()=>filter,setFilter(v){filter=v;page=0;},search(v){query=v;page=0;},next(delta){page=Math.max(0,page+delta);},render(){const selected=rows(),pages=Math.max(1,Math.ceil(selected.length/25));page=Math.min(page,pages-1);return `<div class="title-row"><h1>Clientes AMC</h1><button class="primary" data-action="new-client">＋ Nuevo cliente</button></div><p>En proceso incluye pedidos abiertos, obras pendientes o saldo por cobrar. El historial conserva los trabajos terminados.</p><nav class="section-navigation">${['En proceso','Historial','Archivados'].map(v=>`<button data-action="clients-filter" data-value="${v}" aria-pressed="${v===filter}">${v}</button>`).join('')}</nav><form id="client-search"><label>Buscar por nombre, localidad, teléfono o correo<input name="search" value="${esc(query)}" type="search"></label><button>Buscar</button></form><p>${selected.length} clientes · ${esc(filter)}</p><button data-action="clients-pdf">Descargar PDF de esta lista</button><p class="muted">Podés imprimir el PDF desde tu lector. Incluye todos los resultados del filtro.</p>${selected.slice(page*25,page*25+25).map(c=>`<article class="panel client-card"><div class="title-row"><h2>${esc(c.name)}</h2><span class="status">${c.group}</span></div><p>${esc(c.town||'Sin localidad')} · ${esc(c.phone||'Sin teléfono')}</p>${c.email?`<p>${esc(c.email)}</p>`:'<p class="muted">Cliente administrativo · no necesita cuenta</p>'}<div class="client-summary"><span>${c.requests.length} solicitudes</span><span>${c.quotes.length} presupuestos</span><span>${c.works.length} obras</span></div><button class="primary" data-action="client-budget" data-id="${c.id}" data-lead="${c.hasAccount?0:1}">Nuevo presupuesto</button>${c.hasAccount===0&&getState().clients?.length?`<button data-action="link-client" data-id="${c.id}">Vincular cuenta existente</button>`:``}${c.requests.map(r=>`<a class="hub-row" href="#solicitud/${r.id}">${esc(r.service)} · ${esc(r.status)} →</a>`).join('')||'<p>Sin solicitudes.</p>'}${c.group!=='En proceso'?`<button data-action="client-archive" data-id="${c.id}" data-value="${c.group==='Archivados'?'false':'true'}">${c.group==='Archivados'?'Restaurar al historial':'Archivar cliente'}</button>`:''}</article>`).join('')||'<p>No hay clientes en esta lista.</p>'}<div class="section-navigation"><button data-action="clients-prev" ${page===0?'disabled':''}>Anterior</button><span>${page+1} / ${pages}</span><button data-action="clients-next" ${page===pages-1?'disabled':''}>Siguiente</button></div><p>Archivar limpia el historial visible. No borra cuentas, archivos ni comprobantes.</p>`;}};
}

